name: test
on: [push, pull_request]
jobs:
  node-selfcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --version && node test/node-demo.mjs
      - run: node test/settings-demo.mjs
      - run: npx -y @biomejs/biome@1.9.4 check test/
